# Python Requirements
# This file provides minimal dependencies for testing and CI validation.
# Note: The main application is built with JavaScript/PHP, but these
# dependencies enable Python-based testing infrastructure.
#
# Pin versions as needed for your specific use case:
# Example: numpy==1.24.3

# Core dependencies for potential image processing scripts
numpy>=1.21.0
opencv-python>=4.5.0

# Testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
